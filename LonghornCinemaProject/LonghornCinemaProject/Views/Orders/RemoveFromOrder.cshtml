@model IEnumerable<LonghornCinemaProject.Models.Ticket>

@{
    ViewBag.Title = "RemoveFromOrder";
}

<h2>RemoveFromOrder</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Product Name
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TicketPrice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExtendedPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Showtime.ShowtimeTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TicketPrice)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExtendedPrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", "Ticket", new { id = item.TicketID }, null) |
            @Html.ActionLink("Delete", "Delete", "Ticket", new { id = item.TicketID }, null)
        </td>
    </tr>
}

</table>
