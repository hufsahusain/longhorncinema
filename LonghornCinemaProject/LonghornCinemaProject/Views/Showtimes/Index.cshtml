@model IEnumerable<LonghornCinemaProject.Models.Showtime>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShowtimeTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TheaterNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ShowtimeTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TheaterNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShowtimeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShowtimeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShowtimeID })
        </td>
    </tr>
}

</table>
